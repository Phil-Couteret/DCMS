# DCMS — Roadmap: Current → Target

_Phased plan to go from `current-architecture.md` to `target-architecture.md`. Phases are ordered by risk: security first, then cleanup, then the full multi-tenant data model, then polish. Don't reorder — each phase reduces risk for the next._

## Phase 0 — Stop the bleeding (security, days not weeks)

Do this before any further feature work, and before touching the multi-tenant schema.

**Status: items 1–4 done (2026-07-16). Item 5 intentionally deferred — see note.**

1. ✅ Add `JwtAuthGuard` to every controller currently unguarded — done via a global `APP_GUARD` (`app.module.ts`) plus an explicit `@Public()` decorator (`common/decorators/public.decorator.ts`) on the legitimate exceptions: `AppController` (root/health), `users/login` (+ the `GET /users/login` 405 stub), `partner-auth` (login), and the `partner/*` / `partner-invoices/my-invoices` routes that already enforce `PartnerAuthGuard`/`JwtPartnerGuard`.
2. ✅ Fixed `users.service.ts findAll()`/`remove()` and `partners.service.ts findAll()`/`findOne()`/`update()`/`remove()`/`create()`/`regenerateApiKey()` to strip `password_hash`/`api_secret_hash` from every response.
3. ✅ Removed the hardcoded JWT fallback from all 4 files; added `config/jwt-secret.util.ts` (`getRequiredJwtSecret()`), which throws at boot if `JWT_SECRET` is unset. Set a real generated secret in `backend/.env` and the root `.env` (read by `docker-compose.yml`, which now uses `${JWT_SECRET:?...}` to fail loudly if missing); added `backend/.env.example`. The ZBOX K8s manifests already sourced `JWT_SECRET` from a `Secret` correctly — no change needed there.
4. ✅ `tenant.interceptor.ts` now treats an authenticated admin/staff request's JWT `tenantId` as authoritative: a header/subdomain that disagrees is rejected with 403, and a JWT for a tenant that's since been deactivated is rejected rather than silently falling back to an unscoped view. Superadmins (`tenantId: null`) can still use the header/subdomain to switch tenant context, as originally designed.
5. **Deferred.** CORS is currently broad (LAN CIDR ranges + `*.couteret.fr`) because production runs on a ZBOX reached over the dive center's own local network with varying client IPs — tightening it risks locking out legitimate LAN clients without knowing the real set of origins in use. Revisit with actual deployment topology in hand before changing.

`npx tsc --noEmit` passes clean after these changes. Recommended before deploying: a manual smoke test of login (admin + partner), one request per previously-unguarded controller, and confirming the frontend (which already sends `Authorization: Bearer` + `X-Tenant-Slug` on every request via `httpClient.js`) still works end-to-end.

## Phase 1 — Cleanup & hygiene (low risk, improves everything after it)

**Status: done (2026-07-16).**

1. ✅ `dcms-backend.tar` (173MB, untracked/gitignored) deleted from disk. `Data Saved/Invoice BILL-1767117729046.pdf` removed from the working tree via `git rm` — **note: it was already committed to git history**, so it's still retrievable from old commits until a deliberate `git filter-repo`/BFG history-rewrite pass (out of scope for this pass; needs coordination since it requires a force-push and breaks any other existing clones). `Open Source/` was moved (not deleted) to `docs/planning/` since its content is still-relevant planning material.
2. ✅ Root markdown files consolidated into `docs/setup/`, `docs/guides/`, `docs/database/`, `docs/infra/`, `docs/business/`, and `docs/archive/` (superseded/point-in-time docs, including old POC/sync-server setup notes and a 6.7MB raw Cursor chat-session export that was mislabeled `API_ENDPOINTS_AUDIT.md` — **that file is also already in git history**, same caveat as the invoice PDF above). See `docs/README.md` for the index. `README.md` stays at the repo root as usual.
3. ✅ Added `.env.example` for `frontend` and `public-website` (backend's was added in Phase 0).
4. ✅ Retired `sync-server` entirely: confirmed via `frontend/src/config/apiConfig.js` (`mode: 'api'` hardcoded) that mock mode / sync-server was already dead in every real deployment, then deleted `sync-server/` and removed all references from `start-poc.sh`, `start-network.sh`, `start-standalone.sh`, `check-servers.sh`, `stop-servers.sh`, and their Windows equivalents (`start-windows.bat`, `setup-windows.bat`, `start-standalone.bat`, `check-services.bat`). While in those Windows scripts, also fixed two more hardcoded default JWT secrets (`your-super-secret-jwt-key-change-in-production`) that would have silently defeated the Phase 0 fix for anyone running the Windows setup path — they now generate a random secret via `node -e "...randomBytes..."` the same way Phase 0's `backend/.env` does.
5. ✅ Replaced the 17 `console.*` calls in application runtime code (`main.ts`, `bookings.controller.ts`, `bookings.service.ts`, `partner-bookings.service.ts`, `common/filters/http-exception.filter.ts`) with Nest's `Logger`. Left the 46 `console.log` calls in `backend/src/scripts/*.ts` alone — those are one-off CLI seed/admin scripts, not app runtime, where `console.log` is the normal idiom. While rewriting `http-exception.filter.ts`, also fixed it logging the **raw request body on every error** (including plaintext `password` on failed login) — it now redacts known-sensitive keys before logging.

`npx tsc --noEmit` and `nest build` both pass clean after these changes.

## Phase 2 — Validation & migrations (backend correctness)

**Status: done (2026-07-21).**

1. ✅ Converted every plain-`interface` DTO to a `class-validator` class, across all 24 modules that had one (users, customers, bookings, staff, equipment, boats, boat-preps, locations, dive-sites, government-bonos, settings, customer-bills, breaches, dsar, audit, partners, partner-invoices, partner bookings/customers, consents, tenant, partner-auth). Along the way, consolidated a few modules that had 2–3 duplicate DTO declarations down to one, and fixed inline untyped `@Body()` params in `tenants.controller.ts`. `bookings`'s `activityType` deliberately stays `@IsString()` rather than a strict enum — `BookingsService.mapActivityType()` remaps frontend aliases (e.g. `scuba_diving`) after validation; documented inline in the DTO. `settings`'s `value` field stays untyped/unvalidated — it's genuinely free-form JSON by design.
2. ✅ Flipped `forbidNonWhitelisted: false` → `true` globally in `main.ts` now that every module has a real DTO to validate against, instead of doing it module-by-module (the original plan) — since all modules were converted in the same pass, there was no partial state where some endpoints had DTOs and others didn't. **Not yet smoke-tested against the running frontend** — this is the one Phase 2 item with real regression risk if any frontend flow sends extra fields no DTO expects. `BookingForm.jsx` and `realApiAdapter.js` both have comments suggesting the frontend already assumed strict DTOs, but that needs verifying against the actual running app before this goes to production.
3. ✅ **Prisma migration baseline reconstructed (2026-07-21).** Ran `docs/guides/PRISMA_MIGRATION_BASELINE.md` end to end against a real local Postgres: `prisma migrate diff --from-empty --to-schema-datamodel` generated `backend/prisma/migrations/0_baseline/migration.sql`, then `prisma migrate resolve --applied 0_baseline` marked it applied; `prisma migrate status` confirms "Database schema is up to date!". Along the way, `prisma db pull --print` surfaced a real schema/DB drift: `schema.prisma` declared `onDelete: SetNull` on 9 `tenant_id` relations (`audit_logs`, `certification_agencies`, `customers`, `data_breaches`, `government_bonos`, `locations`, `partners`, `settings`, `users`) that the live DB didn't actually have (it silently defaults to `NO ACTION`). Rather than picking a side unilaterally, presented it as a real product decision — you chose to relax `schema.prisma` to match the DB's current (safer, no-silent-nulling) behavior, so `onDelete: SetNull` was removed from all 9 and re-verified as zero drift.
   - Also found and fixed: `backend/.gitignore` had `/prisma/migrations` gitignored, so **no migration was ever actually committed to git** — not the new baseline, not even the 2 pre-existing ones (`20251227114043_add_partner_invoices`, `20251228212110_add_customer_bills`). A migration history that isn't in version control isn't a real source of truth, so this line was removed and all 3 migrations plus `migration_lock.toml` are now tracked.
   - The old hand-written SQL (`database/schema/`, `database/migrations/`) is now archived to `docs/archive/legacy-sql-migrations/` (see its `README.md` for details, including a flagged duplicate `008_location_type_varchar.sql`/`008_rollback_restore_location_type_enum.sql` pair). `setup-database.sh`, `setup-local-db.sh`, and `backend/docker-entrypoint.sh` were updated to apply the schema via `prisma migrate deploy`/the baseline instead of the archived raw SQL.
   - Fixed a real bug caught while doing this cleanup: `docker/postgres-init/init-dcms.sh` (mounted into the `db` container's `docker-entrypoint-initdb.d`) previously tried to wait for the `backend` container to finish its own schema migration before loading sample data — but `docker-entrypoint-initdb.d` scripts run synchronously during Postgres's own bootstrap, strictly before the container can become healthy, and `backend` has `depends_on: db: condition: service_healthy`. That's a circular deadlock (db won't become healthy until this script finishes; this script waits on backend; backend won't start until db is healthy). Fixed by deleting that script entirely and moving sample-data loading into `backend/docker-entrypoint.sh`, which already runs after schema migration completes in the correct order; `docker-compose.yml`'s `./database:/init:ro` mount moved from the `db` service to the `backend` service accordingly. Also removed dead code from the same entrypoint script (a reference to a nonexistent `/app/scripts/migrations/011_add_multi_tenant.sql`).

`npx tsc --noEmit` passes clean. **2026-07-17, later the same day:** found and fixed an unrelated, uncommitted `schema.prisma` change (`locations.type` had been turned into a fixed 3-value enum, contradicting the already-shipped, documented dynamic Location Types feature — see the "stale multi-tenant admin bootstrap work" commit). That needed a `prisma generate` to pick up cleanly, which you've since run — confirmed clean against the regenerated client.

## Phase 3 — Test safety net (before touching the data model further)

**Status: done (2026-07-17). All three suites written and passing.**

1. ✅ Added backend unit tests for the security-critical code from phases 0–2: `public.decorator.spec.ts` and `jwt-auth.guard.spec.ts` (the `@Public()`/global-guard bypass logic), `tenant.interceptor.spec.ts` (8 cases covering the JWT-authoritative tenant fix — header/JWT mismatch rejection, deactivated-tenant rejection, superadmin platform-level default and header-switch, pre-login and default-tenant fallback), `users.service.spec.ts` and `partners.service.spec.ts` (6 cases each confirming `password_hash`/`api_secret_hash` never appear in any returned object, across every service method), and `create-booking.dto.spec.ts` (a `class-validator`-direct test confirming valid payloads pass, invalid UUID/enum/negative values are rejected, and an unexpected extra field is rejected the same way `forbidNonWhitelisted` rejects it live). All 6 suites (32 tests) pass. Note: `UsersService`/`PartnersService`/`TenantsService` import `bcrypt` directly for password/secret hashing, which ships a native binary compiled for the machine it's installed on — the tests use `jest.mock('bcrypt', ...)` to stub it out, so they run without needing a matching native build.
2. ✅ Wrote and verified `backend/test/unauthenticated-routes.e2e-spec.ts` (4/4 passing) and `backend/test/tenant-isolation.e2e-spec.ts` (6/6 passing).
   - **`unauthenticated-routes.e2e-spec.ts`.** Boots the real app (with `PrismaService` stubbed out, since a rejection happens in the guard before any DB call) and uses Nest's own `DiscoveryService`/`MetadataScanner` to enumerate every registered route, then asserts every route that isn't genuinely credential-free gets a 401 with no `Authorization` header. This distinguishes `@Public()` (skips only the global admin guard) from "no auth at all" — `partner/*`, `partner/bookings`, `partner/customers`, and `partner-invoices/my-invoices` are `@Public()` but carry their own `@UseGuards(PartnerAuthGuard | JwtPartnerGuard)`, discovered generically via Nest's `GUARDS_METADATA`, so they're correctly expected to still 401. Only `AppController` (root/health) and the 3 login handlers are treated as truly open. Because it discovers routes at runtime instead of a hand-maintained list, a new controller or endpoint that forgets a guard is caught automatically. One real bug surfaced and fixed while getting this to pass: e2e tests built via `Test.createTestingModule` skip `main.ts`'s imperative `app.useGlobalPipes(...)` call (only the DI-registered global guard applies automatically), so the login endpoints were reaching their handlers with an unvalidated empty body and 401-ing from their own "invalid credentials" logic rather than 400-ing on missing fields — indistinguishable from a guard rejection. Fixed by replicating `main.ts`'s exact `ValidationPipe` config in the test's `app.init()`.
   - **`tenant-isolation.e2e-spec.ts`.** Seeds two real tenants + a customer each via Prisma, signs real JWTs via `AuthService`, and asserts tenant A's token can't list, read, update, or delete tenant B's customer (all correctly 404, since `CustomersService` filters by `tenant_id` before any write), plus that a header disagreeing with the JWT still gets the Phase 0 403, and that tenant B can still read its own data normally. Verified locally against a real Postgres (Homebrew, on your Mac — separate from both the Zbox's production DB and any Docker Compose setup) once `DATABASE_URL` had a real password and `npx prisma db push` synced the schema. All 6 tests pass.
3. Both suites pass. The intent of this gate — proving the phase 0–2 security fixes actually hold under real requests, not just in isolated unit tests — is met. Phase 4 (`tenant_id` everywhere) is unblocked.

## Phase 4 — Full multi-tenant data model

**Status: code complete (2026-07-21). Not yet applied to any real database — see "before you deploy" below.**

1. ✅ Added `tenant_id` to the 14 tables that didn't have it (`bookings`, `staff`, `equipment`, `boat_preps`, `boats`, `dive_sites`, `customer_stays`, `customer_bills`, `partner_invoices`, `pricing_configs`, `customer_consents`, `customer_certifications`, `data_subject_access_requests`, `bono_usage`) in `schema.prisma`, plus the matching `tenants` back-relations and `idx_*_tenant` indexes. Hand-authored migration `backend/prisma/migrations/20260721140235_phase4_tenant_id_everywhere/migration.sql` (the sandbox this was written in still can't run `prisma migrate dev`/reach a Prisma engine binary — see the Phase 2.3 note above — so this was written directly, following the same DDL shape Prisma itself generates) adds each column, **backfills it from the table's existing `location_id` or `customer_id` relation** (both already resolve to a tenant via `locations.tenant_id`/`customers.tenant_id`), then adds the FK (`ON DELETE NO ACTION`, matching the other 9 tenant_id relations per the Phase 2.3 drift fix) and index.
2. **Deferred — composite uniqueness left as-is.** Per target-architecture.md the ideal is `(tenant_id, username)`/`(tenant_id, email)` on `users`, `(tenant_id, contact_email)` on `partners`, `(tenant_id, key)` on `settings`. Investigated and found this isn't a safe drop-in: `users.service.ts findByUsername()`, `partner-auth.service.ts`, and `settings.service.ts findByKey()` all do `findUnique({ where: { username/contact_email/key } })`, relying on that field being globally unique — with per-tenant uniqueness, two tenants could share a username/email and that lookup becomes ambiguous (or stops compiling against the Prisma client, since a non-unique field can't be a `findUnique` selector). Doing this properly means reworking login/lookup to resolve tenant context (subdomain/header) *before* the query, with no fallback when that context is missing — a real change to login behavior, not a schema tweak. Asked you directly; you chose to keep these three fields globally unique as they are today rather than take on that rework. `bill_number`/`invoice_number`/`code` (bonos) stay globally unique too, unchanged.
3. ✅ **Every service's queries now filter by `TenantContextService`'s tenant** — and this turned up more than just the 14 new tables. Fixed the same gap (previously *zero* tenant filtering) in: `staff.service.ts`, `equipment.service.ts`, `boats.service.ts`, `dive-sites.service.ts`, `boat-preps.service.ts`, `customer-bills.service.ts`, `partner-invoices.service.ts`, `consents.service.ts`, `bookings.service.ts` (switched from an indirect `locations: { tenant_id }` join to the new direct column), and — on the 9 tables that already had `tenant_id` from earlier phases but whose services never used it — `audit.service.ts`, `breaches.service.ts` (data breach records), `government-bonos.service.ts`, `settings.service.ts`, and `statistics.service.ts` (the admin dashboard's revenue/booking counts, previously summed across every tenant). Also fixed a real IDOR in `dsar.service.ts`: `getDsar(dsarId)` looked up a DSAR request by ID alone, ignoring the `customerId` route param entirely, so any authenticated caller could read another customer's (or tenant's) DSAR request by guessing its UUID — now scoped to both tenant and the route's `customerId`. `customer_stays`/`customer_certifications`/`pricing_configs`/`bono_usage` have no dedicated service (read-only via already-tenant-scoped relations, or genuinely unimplemented/unreferenced anywhere in `src/`) — nothing to change there. Every touched module's `*.module.ts` now imports `TenantModule` to get `TenantContextService` injected.
4. ✅ The superadmin cross-tenant switch mechanism itself already existed from Phase 0 (`tenant.interceptor.ts`: a superadmin JWT has `tenantId: null`, and can switch into a tenant via `X-Tenant-ID`/`X-Tenant-Slug`/subdomain). What Phase 4 adds is the audit logging that was missing: every time that switch resolves, a fire-and-forget `audit_logs` entry is written (`action: 'tenant_switch'`, the target tenant, the superadmin's user id, method+path) — fire-and-forget so it never adds latency to the request, and a logging failure can never break the request itself. Covered by a new assertion in `tenant.interceptor.spec.ts` (still 8/8 passing).
5. **Deferred (optional per this plan).** Postgres RLS as defence-in-depth — not started. Application-level filtering (item 3) is the enforcement mechanism for now; revisit RLS once that's run against production for a while.

**Before you deploy this phase:** the new `tenant_id` columns start `NULL` on every existing row. Run the new migration (`cd backend && npx prisma migrate deploy && npm run prisma:generate`) *before* deploying the updated service code — if the code ships first, every tenant-scoped list/read on these 14 tables will suddenly return nothing for existing data, because `where: { tenant_id: <context tenant> }` doesn't match `NULL`. This is the same "backend needs prisma:generate" step as Phase 2.3, plus this migration needs to actually **run** (not just `resolve --applied` like the baseline was) since it changes real data. `npx tsc --noEmit` currently shows exactly 15 errors, all "`tenant_id` does not exist on type ..." on the 14 new tables plus the extended e2e test — confirmed by inspection to be solely the stale generated client, not real bugs; they clear once `prisma generate` is re-run. The `tenant-isolation.e2e-spec.ts` suite was extended with a `staff` case (previously totally unscoped) as a regression guard for this phase — run it locally the same way as Phase 3's.

## Phase 4.6 — Multi-tenant user memberships (2026-07-21, added after Phase 4 shipped)

**Status: code complete. Same "needs `prisma migrate deploy` + `prisma generate` before it works" caveat as above — see migration `20260721141428_user_tenant_memberships`.**

Raised directly by you after Phase 4: some staff legitimately work at more than one diving center. The item 2 deferral above (keeping `users.username`/`email` globally unique) turned out to matter here specifically — with the *old* one-row-per-tenant pattern (duplicate `users` rows, one per center, same as the `admin-<slug>` bootstrap accounts), the same real email address couldn't be used on a second account at all, since `email` is a hard global-unique constraint. That's a real, not hypothetical, blocker.

Rather than patch the constraint (which only helps `email`, not the deeper "one person, one login" expectation), added a proper many-to-many model:

1. New table `user_tenant_memberships` (`user_id`, `tenant_id`, `role`, `permissions`, `location_access`, `is_active`) — purely additive, `ON DELETE CASCADE` on both FKs (this table is an access grant, not a business record, so cascading is correct here unlike the `NO ACTION` convention used elsewhere). `users.tenant_id`/`role`/`permissions`/`location_access` stay exactly as they are today and remain each user's *primary* tenant — a person with only one tenant (still the common case) needs nothing beyond that, zero behavior change. `username`/`email` stay globally unique, since a `users` row is now unambiguously "one real person," not "one tenant relationship."
2. `users.service.ts login()`: after password verification, gathers the user's available tenants (primary + any active memberships). Superadmins are untouched (still `tenantId: null`, still switch via header/subdomain per Phase 4 item 4). A regular user with exactly one tenant option logs in immediately, same as before Phase 4.6 existed. A user with 2+ options and no subdomain/header narrowing it down gets `{ requiresTenantSelection: true, tenants: [...] }` back instead of a token — no login attempt is fully authenticated until a tenant is chosen. New endpoint `POST /users/login/select-tenant` (`{ username, password, tenantId }` — re-checks the password rather than inventing a separate short-lived token type, simpler to reason about for one extra `bcrypt.compare()`) completes it, issuing a JWT scoped to that membership's own role/permissions (which can differ per tenant - e.g. instructor at Center A, guide at Center B).
3. Membership management: `POST/DELETE/GET /users/:id/tenant-memberships` to grant, revoke, and list a user's tenants.
4. Frontend: `AdminLogin.jsx` shows a "which center?" popup (per your request for something along those lines) when `login()` returns `requiresTenantSelection`, then completes login via the new endpoint once one is picked.
5. New unit tests in `users.service.spec.ts` cover the selection prompt, successful tenant selection with per-membership role/permissions, and rejection of a tenantId the user has no membership for.

**Done (follow-up):** post-login "switch center" affordance. New authenticated `POST /users/switch-tenant` endpoint (`{ tenantId }`, no password - identifies the user from their existing JWT and re-validates the requested tenant via the same `getTenantOptions()` used by login) plus a "Switch Center" item in `Navigation.jsx`'s user menu, shown only when the user has 2+ tenant options. Selecting one re-tokens and reloads the app. Superadmins are unaffected - they keep their separate header/subdomain switch mechanism.

## Phase 5 — Frontend & public-website polish

1. ✅ **`Settings.jsx` split (2026-07-21).** `Settings.jsx` was a single ~3,544-line file covering 9 unrelated tabs (Organisation, Locations, Location Types, Prices, Dive Sites, Boats, User Management, Partners, Certification Verification). Split into 8 new self-contained components under `components/Settings/` (`OrganisationSettings`, `LocationsManagement`, `LocationTypesManagement`, `DiveSitesManagement`, `BoatsManagement`, `UserManagement`, `PartnersManagement`, `CertificationVerification`) — each owns its own state, data loading, and dialogs, matching the pattern `Prices.jsx`/`TenantManagement.jsx` already used before this refactor (rather than introducing a new shared state layer, which would have been a bigger and riskier change than "split into smaller components" called for). `Settings.jsx` itself is now a ~110-line tab router. Given the size (~8,600 lines moved) and that this sandbox can't click through the actual UI against a live backend, this was done as a pure "Extract Component" refactor — content moved verbatim, no logic changes — and verified three ways: `@babel/parser` on every new file, a new `Settings.smoke.test.jsx` that mounts the real page and clicks through all 8 tenant-admin tabs plus the superadmin path (3 tests, catches broken imports/props/JSX nesting a parser can't), and the full existing suite (33 tests total, all passing).
1b. ✅ **`BoatPrep.jsx` split (2026-08-10).** `BoatPrep.jsx` was a single ~2,721-line file whose 3 tabs (Dive Preparation, Post-Dive Reports, Compliance Reports) share ~1,450 lines of tightly-coupled state (boat/staff/diver assignments, dive-site suggestions, sync with Schedule bookings) — unlike `Settings.jsx`'s independent tabs, splitting this into fully self-contained components would have meant duplicating that logic three times or silently diverging it. Per your call, used a different pattern: a new `hooks/useBoatPrepData.js` (1,555 lines) centralizes all state/effects/handlers/module-level helpers as a single hook returning one object; the 3 tabs became presentational components under `components/BoatPrep/` (`DivePreparationTab`, `PostDiveReportsTab`, `ComplianceReportsTab`) that each take the hook's return value as props. `BoatPrep.jsx` itself is now a ~25-line router: calls `useBoatPrepData()` once, renders `<Tabs>`, and spreads the result into whichever tab is active. Given the scale and that this is dive-safety-adjacent logic (staff certification requirements, boat capacity, diver skill matching), extraction was done via scripted verbatim line extraction (`sed`) rather than manual retyping, and dependencies (which of the hook's ~116 returned names each tab actually uses) were determined programmatically via `@babel/parser`/`@babel/traverse` scope analysis rather than by eye — this caught a real gap (the hook's `renderDiverItem` helper returns JSX and needed `Box`/`IconButton`/`ListItem`/`ListItemText`/`DeleteIcon` imports that a manual pass could easily have missed). Verified four ways: `@babel/parser` on every new/changed file, a babel scope-analysis pass confirming zero unresolved identifiers in any of the 5 files, a new `BoatPrep.smoke.test.jsx` that mounts the real page and switches between tabs (3 tests), and the full existing suite (36 tests total, all passing). You tested it locally against seeded data (`npm run seed:boatprep-test`, added as part of this verification) and confirmed it renders correctly with real staff/boat/booking data.
1c. ✅ **`Financial.jsx` split (2026-08-10).** `Financial.jsx` (2,370 lines) had the same shared-state shape as `BoatPrep.jsx` — its 4 tabs (Current Financial, Previous Closed Days, Historical Bills, Quarterly Tax/IGIC Declaration) share state and data-loading effects gated by `activeTab`, with the added wrinkle that which numeric tab index means what shifts depending on `isBikeRental` (bike-rental locations skip the Closed Days tab). Split with the same shared-hook pattern: `hooks/useFinancialData.js` (1,148 lines) centralizes all state/effects/handlers/HTML-report-generation logic; the JSX became 5 presentational components under `components/Financial/` — `CurrentFinancialTab`, `PreviousClosedDaysTab`, `QuarterlyTaxDeclarationTab`, `HistoricalBillsTab`, and `FinancialDialogs` (the 5 dialogs — expense/income/close-day/view-bill/view-report — render as always-mounted siblings after the tabs, same as in the original, rather than being force-fit into a single tab). `Financial.jsx` itself is now a ~49-line router. Same scripted-extraction + babel-AST dependency analysis method as `BoatPrep.jsx`. This pass caught two real pre-existing issues, both fixed as part of the extraction (not left broken, since they're one-line-equivalent and unambiguous):
   - `expenseCategories.find(...)` referenced an **undeclared variable** in two places (the daily-report HTML generator and the Current Financial tab's expense table) — `expenseCategories` was never declared anywhere in the original file; every location with expenses recorded for a given day would have hit a `ReferenceError` rendering that section. Fixed to call the existing `getExpenseCategories()` function instead, matching its other use site.
   - `QuarterlyTaxDeclarationTab.jsx` used `date-fns`'s `format()` directly in JSX without importing it — caught by the same babel scope-analysis pass used for `BoatPrep.jsx`.
   Verified the same four ways as `BoatPrep.jsx`: babel parse + scope analysis (zero unresolved identifiers across all 7 new/changed files), a new `Financial.smoke.test.jsx` (4 tests — mounts the real page, switches through all 4 tabs, and specifically exercises the `getExpenseCategories()` fix by rendering a mocked expense row), and the full suite (40 tests total, all passing). One test-writing gotcha worth recording: CRA's Jest config sets `resetMocks: true`, which silently clears `.mockResolvedValue()`/`.mockReturnValue()` set inside a `jest.mock()` factory before every test — return values have to be (re)configured in `beforeEach` instead, or a mock silently resolves to `undefined`. This cost real debugging time and is now called out in the new test file's comments for future reference.
   Phase 5.2 (the three oversized-file splits) is now complete.
2. ✅ **Frontend tests for `ProtectedRoute`/`authContext` and the login flow (2026-07-21).** The frontend had zero test coverage before this — added `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event` as devDependencies (none were present; CRA's own template installs them, but this project's `package.json` never had them) plus `src/setupTests.js` to wire up the `jest-dom` matchers. New suites: `authContext.test.js` (17 tests — `login`/`logout`/localStorage persistence, `canAccess`'s superadmin bypass and settings-requires-global-scope rule, and the pure `hasPermission`/`hasLocationAccess`/`isMultiLocationUser` helpers), `ProtectedRoute.test.jsx` (7 tests — loading state, mock-mode vs API-mode unauthenticated views, the "user exists but no JWT" edge case, and permission-gated access), and `AdminLogin.test.jsx` (6 tests — single-tenant login, the Phase 4.6 "which center?" multi-tenant popup end-to-end including `select-tenant`, and both server-error and network-error messaging). All 30 tests pass (`CI=true npx react-scripts test --watchAll=false`). Deliberately scoped to the auth-critical path rather than every page — "core pages" (Dashboard, Bookings, etc.) remain untested; worth a follow-up but lower risk than the login/permission gate itself.
3. `public-website`: switch `HashRouter` → `BrowserRouter`, add SEO tags/`robots.txt`/`sitemap.xml`, route or delete orphaned pages.
4. ✅ Migrate both React apps from Create React App to Vite. **Done, 2026-08-11 — `frontend` in Phase 6 item 18, `public-website` in item 19** (same day, done as two separate changes rather than one — `frontend` first since it had a real test suite to verify against).

## Phase 6 — Hardening & operational maturity

Found during a full architecture review (2026-08-10), after Phase 5.2 closed out the oversized-file work. These aren't security holes on the scale of Phase 0, but they're the next layer down: dependency freshness, abuse resistance, container hardening, disaster recovery, and the process gaps (no CI, thin test coverage outside the security-critical path) that make regressions more likely to slip through going forward.

1. ✅ **Dependency vulnerabilities (2026-08-10).** `npm audit fix` (non-breaking) run across all four `package.json`s (root, `backend`, `frontend`, `public-website`). Backend: resolved the one critical finding (`body-parser`) plus most highs; one moderate remains (`@nestjs/core` injection advisory) that needs a NestJS 10→11 major-version bump — deliberately deferred rather than force-applied, since that's a breaking change needing its own test pass, not something to bundle into a routine audit fix *(**resolved 2026-08-11, see item 17**)*. Frontend: 58→30 findings; the remainder is almost entirely inside `react-scripts`' own pinned build-tool chain (webpack-dev-server, postcss, etc.) — `react-scripts` lives in CRA's `dependencies` (not `devDependencies`, which is CRA's own convention) so `npm audit --omit=dev` doesn't actually exclude it, but that code is a build tool that never ships in the static `build/` output actually served to users; the one real production-shipped issue (`react-router`, moderate open-redirect) also needs a major v6→v7 bump, deferred the same way *(**resolved 2026-08-10, see item 16** — turned out not to need the Vite migration after all, see that item for why)*. `public-website`: same pattern, 32 remaining findings, same reasoning *(**resolved 2026-08-11, see item 19** — down to 1 unrelated pre-existing finding, `sharp`, after the CRA→Vite migration)*. **The NestJS major-version bump, and the broader false-positive noise from CRA's dependency classification, are best resolved together by the CRA→Vite migration already on this list (item 4 above) rather than patched piecemeal.** Verified after fixing: backend `tsc --noEmit` clean, 35/35 backend tests pass, 40/40 frontend tests pass, and all three apps (`nest build`, both `react-scripts build`) build successfully.
   - **Also fixed as part of this: `package-lock.json` was gitignored repo-wide (all four of them — root, backend, frontend, public-website — were untracked).** This meant `npm audit fix` only ever fixed a given machine's local `node_modules`, not the repo — the next `npm install` anywhere else (Docker build, ZBOX deploy, another developer's clone) could silently re-resolve to different, potentially still-vulnerable transitive versions, and there was no way to get a reproducible install. Removed the `package-lock.json` line from `.gitignore` and committed all four lockfiles. This is arguably the more important fix of the two — without it, today's audit-fix work wouldn't have outlived the sandbox session.
2. ✅ **Rate limiting + security headers (2026-08-10).** Added `@nestjs/throttler` (v6.5.0, compatible with the current Nest 10.x — no framework bump needed) and `helmet` (v8.3.0). App-wide default: 100 requests/60s per IP via a global `ThrottlerGuard` registered as a second `APP_GUARD` alongside the existing `JwtAuthGuard`. The three credential-checking endpoints (`POST /users/login`, `POST /users/login/select-tenant`, `POST /partner-auth/login`) get a tighter `@Throttle` override — 10/60s — since those are what brute-force/credential-stuffing actually targets, not the general API. `helmet()` applied in `main.ts` with `contentSecurityPolicy: false` (the Swagger UI served from the same app needs inline scripts/styles; revisit if Swagger ever moves to its own origin). Caught and fixed a real regression risk before it shipped: `unauthenticated-routes.e2e-spec.ts` fires 100+ sequential requests (one per registered route) from a single test client to verify the auth guard — with the new global throttle active it would have started returning 429 partway through and been indistinguishable from a route that failed to reject an unauthenticated request. Fixed by overriding `ThrottlerGuard` to a no-op for that specific suite (`.overrideGuard(ThrottlerGuard).useValue({ canActivate: () => true })`) since it's testing auth, not rate limiting. Added a new `login-rate-limit.e2e-spec.ts` that fires 12 login attempts against a stubbed Prisma and asserts the first 10 get 401 (bad credentials) and at least one of the last 2 gets 429. Verified: `tsc --noEmit` clean, 35/35 unit tests pass, both relevant e2e suites pass (`login-rate-limit`, `unauthenticated-routes`), `nest build` succeeds.
3. ✅ **Docker non-root (2026-08-10).** Backend: switched the final stage to run as the `node` user the official `node:20-bullseye-slim` image already provides (uid 1000), rather than root — added `RUN chown -R node:node /app` (needed since files land as root during `COPY`) before `USER node`. Nothing in `docker-entrypoint.sh` or the app needs root: no privileged port (3003), no writes outside `/app`, and the one external mount (`/init/seeds`, docker-compose only) is read-only. Frontend and public-website were the harder case — both serve via `nginx:alpine` on port 80, and nginx's stock image needs root just to bind that privileged port, so a bare `USER` directive would have broken the container rather than hardened it. Switched both to `nginxinc/nginx-unprivileged:alpine`, the official variant built to run entirely as a non-root user (uid 101) on an unprivileged port (8080) with no further config needed. Updated the one call site each: `docker-compose.yml` (`3000:8080`), and `deploy/zbox/k8s/admin.yaml` / `public.yaml` (`containerPort`/`targetPort` → `8080`; the Kubernetes `Service`'s external `port: 80` and the ingress's `service.port.number: 80` are unaffected — only the container-internal port moved). **Verified (2026-08-10, on the developer's actual machine with Docker available):** `docker compose up -d --build` built and started `db`/`backend`/`frontend` cleanly; `docker compose exec backend whoami` → `node`, `docker compose exec frontend whoami` → `nginx` (neither `root`); `curl -I` against both `http://localhost:3003/api` and `http://localhost:3000` returned `200 OK`, confirming the app actually works non-root, not just that the containers start. `public-website` (not wired into `docker-compose.yml`) built and checked separately the same way — `docker build -t dcms-public-website ./public-website` succeeded, `whoami` inside the running container returned `nginx`, and `curl -I` returned `200 OK`. This closes the "not verified" gap this item originally carried.
4. ✅ **Automated production database backups (2026-08-10).** Added a `postgres-backup` Kubernetes `CronJob` (`deploy/zbox/k8s/postgres-backup-cronjob.yaml`) running `pg_dump --format=custom` daily at 03:15, writing to a separate PVC (`postgres-backup-pv.yaml`, `/data/dcms/postgres-backups` — deliberately a different host directory than the live DB's `/data/dcms/postgres`, so a bad query or accidental deletion in the live data doesn't also destroy the backups sitting next to it) and pruning dumps older than 14 days. Same `postgres:15-alpine` image as the live DB so `pg_dump`'s version always matches what it's dumping. Added `deploy/zbox/scripts/restore-backup.sh` (`list` / `restore <file>`) — spins up a one-shot pod mounting the backup PVC read-only, runs `pg_restore --clean --if-exists --no-owner`, requires typing the filename back to confirm before touching the live DB. Wired both new manifests into `deploy.sh` (host directory creation/chown, `kubectl apply`) and documented the whole flow in `deploy/zbox/README.md`. **Known remaining gap, called out explicitly rather than silently left out:** backups live on the same physical disk as the live database (different directory, same disk) — this protects against accidental deletion/corruption but not hardware failure. Off-box replication (rsync/rclone to another machine or object storage) wasn't added since it depends on what remote target is actually available, which wasn't known at the time; flagged in the README as the next step.
5. Split `Equipment.jsx` (2,240 lines — larger than `BoatPrep.jsx` was), `Schedule.jsx` (1,588), `Bill.jsx` (1,196) — same file-splitting pattern as Phase 5.2, not originally on that list.
   - 5a. ✅ **`Equipment.jsx` (2026-08-10).** Same shared-hook + presentational-tabs pattern as `BoatPrep.jsx`/`Financial.jsx`: `hooks/useEquipmentData.js` (all state/effects/handlers for both the Equipment/rental-inventory list and the Tanks/Cylinders testing tracker, which share permission checks and can each trigger the other's reload) plus `components/Equipment/{EquipmentTab,TanksTab}.jsx` (presentational). `pages/Equipment.jsx` is now a 46-line tab router + shared `Snackbar`. Extraction used the same verbatim `sed` line-range extraction and babel scope-analysis verification as Phase 5.2 (catching two missing `Alert` imports this time, both fixed before commit — no undeclared-variable bugs found in this file, unlike Financial.jsx). Added `Equipment.smoke.test.jsx` (3 tests: default tab renders and shows loaded equipment, switches to Tanks tab, switches back). Verified: `react-scripts build` compiles clean (bundle size effectively unchanged, as expected — no lazy-loading added yet, see item 10), full frontend suite 43/43 passing (was 40/40 before this).
   - 5b. ✅ **`Schedule.jsx` (2026-08-10).** Different shape than the other three splits: Schedule switches "views" (month/daily/week) via plain `Button`s rather than MUI `Tabs`, and already had two subcomponents (`DayDetailView`, `SlotDetailView`) defined as separate top-level consts lower in the same file rather than nested in the main component. Split into `hooks/useScheduleData.js` (all state/effects/handlers), `utils/scheduleConstants.js` (mole/boat slot timing constants, previously module-level consts both the main component and `SlotDetailView` relied on via same-file scope), three view components (`MonthView`/`DailyView`/`WeekView`) under `components/Schedule/`, and `DayDetailView`/`SlotDetailView` relocated to their own files in the same directory. The header (view toggle, date nav) and the two dialogs stayed in `pages/Schedule.jsx` (now 257 lines) rather than being extracted further, since they're tightly coupled to `viewMode`/`selectedDate`/`selectedSlot` and small enough that extracting them would have meant threading most of the hook's return value through anyway for no real gain. **Found two live, pre-existing bugs during extraction, both fixed:** `DayDetailView` called `navigate(...)` (Mole section's "Trip Details" button) despite `navigate` never being in scope — it's a separate top-level component, not nested in `Schedule`, so `const navigate = useNavigate()` in the main component was never visible to it; this ran every time the day-detail dialog opened (the component is always invoked with `slot.type: 'day'`). `SlotDetailView` called `onRemoveBoatAssignment(...)` (a boat slot's assigned-customer chip delete handler) despite never declaring it as a prop — the boat-slot dialog call site passed it, but the component itself dropped it on the floor, so removing an assigned customer from a boat slot threw a `ReferenceError`. Both are the same category of bug as Financial.jsx's `expenseCategories` (Phase 5.2): identifiers that read fine at a glance because they *looked* like they were in some enclosing scope, but weren't. Fixed by calling `useNavigate()` directly in `DayDetailView` (it's its own module now) and adding `onRemoveBoatAssignment` to `SlotDetailView`'s prop destructure; both fixes documented inline. Also found a third, latent (never-reachable) instance of the same pattern — `DayDetailView`'s dead `slot.type !== 'day'` branch referencing `staff`/`slotGuides`/`onUpdateGuides` — fixed defensively (accepted as props, passed through from the dialog) even though the only call site always passes `slot.type: 'day'`. Added `Schedule.smoke.test.jsx` (3 tests, wrapped in `MemoryRouter` since the hook calls `useNavigate()`). Verified: `react-scripts build` compiles clean, full frontend suite 46/46 passing (was 43/43 before this).
   - 5c. ✅ **`Bill.jsx` (2026-08-10).** The simplest of the three shapes: no tabs, no view modes, just a single printable invoice. Split into `hooks/useBillData.js` (all state/effects/handlers, including the pricing calculation and the partner-invoice-creation effect) and two presentational components under `components/Bill/`: `BillActions.jsx` (the small header bar - back/email/print/download) and `BillDocument.jsx` (the printable invoice itself - company header, customer info, itemized activities/extras, totals, and the "Close Stay" action). `pages/Bill.jsx` is now 38 lines. One deliberate deviation from the pattern used elsewhere: the Close Stay button's `onClick` is a ~230-line handler (create partner invoices, mark the stay billed in `localStorage`, update every booking with the bill reference) that was inline JSX in the original file rather than a named function - given it's money-handling logic, it was moved into `BillDocument.jsx` verbatim via the same `sed` extraction as everything else rather than manually restructured into a named hook function, to avoid any transcription risk. Babel scope-analysis (same verification method as the other two splits) caught a missing `Button` import and a missing `setPartnerInvoicesCreated` prop in `BillDocument.jsx`, both fixed before commit - no undeclared-variable bugs this time, unlike Financial.jsx/Schedule.jsx. Added `Bill.smoke.test.jsx` (1 test - mounts the page with a stay passed via router location state, the same way the real Stays page navigates here, and waits for the calculated invoice to render). Verified: `react-scripts build` compiles clean, full frontend suite 47/47 passing (was 46/46 before this). **This closes out roadmap item 5** (`Equipment.jsx`/`Schedule.jsx`/`Bill.jsx`).
6. ✅ **Test coverage — targeted expansion (2026-08-10).** Scoped deliberately (user chose "targeted" over "comprehensive" when asked): the highest-risk untested logic, not exhaustive coverage of every service/page. Backend: `bookings/bookings.service.spec.ts` (25 tests, NEW) — Bookings is the most central business entity (every booking sets what a customer is charged) and had zero tests; covers tenant scoping (`tenantFilter()`), the `mapActivityType()` frontend-alias-to-Prisma-enum mapping including its "unknown type silently defaults to diving" fallback, the `"account"` → `"deferred"` payment-method remap (independently reimplemented in both `create()` and `update()`), and the customer/location existence checks in `create()`. Frontend: `services/pricingService.test.js` (30 tests, NEW) — pure calculation logic with zero network calls that both `Bill.jsx` and `Financial.jsx` depend on, covers `calculateDivePrice`'s tier-boundary selection (including out-of-order tier config), the pack-price-overrides-tiered-pricing precedence in `calculateDivePriceWithPacks`, and all the hardcoded fallback prices used when a location has no pricing configured. `pages/Customers.smoke.test.jsx` (3 tests, NEW) — at 950 lines, bigger than several of the pages already split in Phase 6.5, and had zero tests despite being core CRUD; smoke-tests the list load, the search flow, and the `?mode=new` form-vs-list branch. Verified: backend 60/60 passing (was 35), frontend 80/80 passing (was 47). Ordinary CRUD elsewhere (staff, equipment, partners beyond what Phase 6.5's splits already covered) remains untested — deliberately out of scope for "targeted," left for a future pass if it's requested.
7. ✅ **CI pipeline (2026-08-10).** Added `.github/workflows/ci.yml` — three parallel jobs (backend, frontend, public-website), each running on push to `main` and on every PR: install, tests, a production build, and a dependency audit. Audit gate is `--audit-level=critical` for all three (not `high`) — deliberately: backend and frontend both currently carry accepted high/moderate findings that need major-version bumps (NestJS 10→11 for backend, mostly `react-scripts`' own build-tool chain for frontend/public-website), already documented and deferred in Phase 6.1; gating on `high` would make CI red on day one for known, tracked debt instead of catching new regressions, which defeats the purpose. **Found and fixed a genuinely new critical finding while wiring this up:** `bcrypt` 5.x's dependency chain (`@mapbox/node-pre-gyp` → `tar`) had picked up new `node-tar` advisories since Phase 6.1's audit pass, at critical severity — bumped `bcrypt` `^5.1.1` → `^6.0.0` (backend's only two call sites are `bcrypt.hash(str, rounds)` and `bcrypt.compare(plain, hash)`, both stable across v5→v6; the on-disk `$2b$` hash format is unchanged, so existing password hashes remain valid). This is a real dependency fix riding along with the CI work, not a demonstration of the pipeline — it's what made the `critical` gate meaningful to add in the first place, since it would otherwise have failed immediately. Also matched two details other parts of this repo had already hit and solved: the `.bin/react-scripts` wrapper that can fail depending on install method (`frontend/Dockerfile` works around it; the CI frontend/public-website build steps use the same `node .../scripts/build.js` invocation), and `CI=false` for the build step specifically (GitHub Actions sets `CI=true` by default, which makes CRA treat existing ESLint warnings as build-failing errors — not something to silently paper over as a side effect of adding CI). `public-website`'s test step needed `--passWithNoTests` (it has no test files yet, a real gap, not something to fix here). **Verification note:** backend's build/test steps could not be fully dry-run end-to-end in the sandbox this was done in — its network proxy blocks `binaries.prisma.sh` (returns 403), so `prisma generate` can't fetch the query engine binary here, which cascaded into a broken local Prisma client after an unrelated `npm ci` attempt earlier in the same debugging session. This is a sandbox-specific restriction, not a real limitation - GitHub Actions runners have normal internet access. The backend's 60-test unit suite (all of which mock `PrismaService` directly, per `bookings.service.spec.ts`/`partners.service.spec.ts`/`users.service.spec.ts`) was already verified passing earlier in this same work session, before the `bcrypt` bump; frontend and public-website's test/build/audit steps were verified directly against the exact commands used in the workflow. The workflow YAML itself was validated for syntax. **First real CI run on push is the actual verification of the backend job** — flagged clearly rather than presented as already fully proven.
8. ✅ **Pagination for unbounded list endpoints (2026-08-10).** Added opt-in `skip`/`take` query params to the five endpoints that previously returned every row with no limit at all: `bookings` (`findAll`/`findByDate`/`findByCustomer`), `staff` (`findAll`/`findByLocation`), `boats` (`findAll`/`findByLocation`), `equipment` (`findAll`/`findByLocation`/`findAvailable`), `dive_sites` (`findAll`/`findByLocation`). New shared helper `common/utils/pagination.ts` (`parsePaginationQuery`) parses the two query-string values into `{ skip?, take? }`, silently dropping anything non-numeric, negative, or zero rather than passing garbage to Prisma, and caps `take` at 500 even when a caller opts in. **Deliberately not the `audit`-module pattern** (`{ logs, total, limit, offset }` envelope): `frontend/src/services/dataService.js`'s `getAll(resource)` is called throughout the frontend (Schedule.jsx, Equipment.jsx, Bill.jsx, Customers.jsx, BoatPrep.jsx, Financial.jsx) and always treated as a plain array, and several of those pages (Schedule.jsx's date-range view, Financial.jsx's aggregation) rely on getting the *complete* unfiltered list back for correct client-side math — an envelope response or a bounded default would have been a large breaking change, and a bounded default specifically risked silently wrong totals rather than a visible error. So this ships as zero-blast-radius: when `skip`/`take` are omitted (true for every existing call site today), `parsePaginationQuery()` returns `{}`, spread into `findMany()` as `...pagination` is a complete no-op, and the response is byte-for-byte what it was before this change. A future paginated UI or API integration can opt in per-request without anyone else's behavior changing. Added `common/utils/pagination.spec.ts` (8 tests, pure logic, no Prisma dependency) plus three new tests in `bookings/bookings.service.spec.ts` asserting `findAll()` passes no `skip`/`take` keys at all to Prisma when omitted (not just `undefined` values, which could still trip up a stricter mock) and that they're forwarded correctly when provided. **Verification note:** this sandbox's `prisma generate` is still blocked the same way documented in item 7 (proxy blocks `binaries.prisma.sh`), so the full backend suite/build couldn't be run end-to-end here. Instead verified via `tsc --noEmit` against both the working tree and a `git stash`-baseline: exactly 42 pre-existing type errors in both cases, all originating from the same broken-stub-client issue (enum types like `activity_type`/`payment_method` not exported), none introduced by this change, none touching the new `common/utils/` files. The new `pagination.spec.ts` (pure logic, no Prisma import) ran and passed cleanly (8/8) directly in this sandbox. `staff`/`boats`/`equipment`/`dive-sites` have no existing `.spec.ts` files to extend (consistent with item 6's "targeted" scope, which prioritized `bookings`) — a gap that predates this change, not introduced by it.
9. ✅ **Structured JSON logging (2026-08-10).** Given the user's choice between JSON logs only vs. JSON logs + Sentry (asked explicitly, since this app already handles customer PII under its consents/DSAR/breach-notification modules, so routing error payloads to a third-party SaaS is a bigger call than the other Phase 6 items): **JSON logs only, no external service.** Added `nestjs-pino`/`pino-http`/`pino` (`pino-pretty` as a dev dependency only). Wired via `LoggerModule.forRoot({ pinoHttp: buildPinoHttpOptions() })` in `app.module.ts` and `app.useLogger(app.get(Logger))` in `main.ts` (with `bufferLogs: true` on `NestFactory.create` so early framework startup logs aren't lost before `useLogger` runs) — this means every existing `new Logger(ctx)` call across the codebase (7 call sites: services, `AllExceptionsFilter`, bootstrap) automatically routes through structured JSON now, with **zero call sites needing to change**, since Nest's built-in `Logger` delegates to whatever logger was registered via `useLogger`. `pino-http`'s `autoLogging` also adds one JSON line per completed HTTP request (method/url/status/response-time) for free — something the app had no equivalent of before. Level defaults to `info` in production / `debug` otherwise, overridable via a new `LOG_LEVEL` env var (documented in `backend/.env.example`); output is plain JSON to stdout in production (matches how container logs are already collected, ready to feed a future Loki/ELK aggregator with no further code change) and colorized `pino-pretty` output in development. Redacts `req.headers.authorization`, `req.headers.cookie`, and `res.headers["set-cookie"]` at the pino-http level — separate from and in addition to `AllExceptionsFilter`'s existing request-body redaction on the error path. The level/redact/transport logic was extracted into `common/logger/pino-http.config.ts` (`buildPinoHttpOptions(env)`) specifically so it's unit-testable without booting the full Nest app (which needs a real Prisma client — see the sandbox limitation noted in items 7/8); `common/logger/pino-http.config.spec.ts` (4 tests) covers the prod/dev defaults, `LOG_LEVEL` override, and the redact paths. No k8s/docker-compose manifest changes needed — `NODE_ENV=production` is already set in both (`docker-compose.yml`, `deploy/zbox/k8s/backend.yaml`), so JSON output is automatic. **Verified:** `tsc --noEmit` unchanged at the same 42 pre-existing (Prisma-stub-only) errors as items 7/8, none newly introduced; `pino-http.config.spec.ts` and `pagination.spec.ts` (both Prisma-independent) ran and passed in-sandbox (12/12); `npm audit --omit=dev --audit-level=critical` clean after adding the three new runtime dependencies. Full app boot/request-flow (actually seeing a JSON log line emitted) not verified end-to-end in this sandbox for the same blocked-Prisma-client reason as items 7/8 — worth a quick manual check (`npm run start:dev` and hit an endpoint) on a machine where `prisma generate` succeeds, before relying on this in production. Sentry/external error-tracking explicitly deferred, not rejected — revisit if alerting (not just log aggregation) becomes a real need.
10. ✅ **Frontend route-based code splitting (2026-08-10).** Converted all 16 page-level imports in `App.jsx` (`Dashboard`, `Bookings`, `Stays`, `Customers`, `Equipment`, `Settings`, `BoatPrep`, `Schedule`, `TripDetails`, `Breaches`, `Bill`, `Partners`, `PartnerInvoices`, `Bills`, `Financial`, plus the partner-portal's `PartnerLogin`/`PartnerDashboard`) from static `import X from './pages/X'` to `React.lazy(() => import('./pages/X'))`, each now its own chunk fetched only when its route is actually visited rather than all bundling into the single main chunk up front. Two `<Suspense>` boundaries, both showing the same `CircularProgress` spinner `ProtectedPartnerRoute` already used elsewhere in the app (matching an existing pattern rather than introducing a second loading-state style): one at the top level (needed for `PartnerLogin`/`PartnerDashboard`, which render outside the admin shell) and one inside the admin shell's `Box` wrapping just the nested page `Routes` — so navigating between admin pages shows the spinner only in the content area while `Navigation`'s sidebar stays mounted, rather than blanking the whole screen. **Result:** main bundle dropped from 436KB to 176KB gzipped (-261KB, -60%), split into ~30 chunks (`943.*.chunk.js` at 111KB is the next-largest — very likely MUI/date-picker/chart vendor code shared across pages rather than a single page, would need `webpack-bundle-analyzer` to confirm precisely, not done here). Verified: `react-scripts build` compiles clean with no new warnings, full frontend suite still 80/80 passing (unchanged from item 6/7 — the smoke tests import pages directly, not through `App.jsx`, so lazy-loading doesn't affect what they exercise).
11. ✅ **Repo hygiene (2026-08-10).** `data tanks.csv` (real tank inventory — serial numbers, hydrostatic/visual test dates, storage notes; a `docs/archive/` chat export shows it was a one-off bulk-import attempt that landed at the repo root by accident, not a deliberate fixture) — asked the user how to handle it given it's real operational data, not sample data; they chose removal. `git rm`'d from the working tree; still present in git history at commit `9157c30` (removing it from history is a separate, heavier `git filter-repo`/BFG operation requiring a force-push, same caveat already on file for `Data Saved/Invoice BILL-1767117729046.pdf` and the Cursor chat export in `current-architecture.md` §10 — not attempted here since it wasn't asked for and the data is business/operational, not customer PII on the same order as the invoice). `current-architecture.md` §5 (frontend structure) rewritten — it still described the pre-Phase-5.2/6.5 state (17 flat page files, "no hooks/ folder", "zero frontend tests found"), none of which has been true since this session's earlier work; now reflects the six split pages, `hooks/`, 43 component files, and the 80-test suite from item 6, with the "targeted not comprehensive" caveat repeated so it doesn't read as full coverage. Also added an entry to §10 documenting the `data tanks.csv` removal alongside the other flagged-but-not-purged history items.
**Post-session verification (2026-08-10, on the developer's actual machine, not the sandbox):** items 7, 8, and 9 above each carried the same caveat — this sandbox's network proxy blocks `binaries.prisma.sh`, so `prisma generate` couldn't fetch a real query-engine binary, and verification for those three items was limited to `tsc --noEmit`/isolated unit tests instead of a real `nest build` + full suite run. That gap is now closed: a clean `rm -rf node_modules && npm install` followed by `npm run build` and `npm test` on the actual development machine (which has normal internet access) produced `nest build` compiling with no errors and **75/75 backend tests passing** (the 60 from item 6, plus exactly the 15 new tests added across items 8/9: 8 in `pagination.spec.ts`, 4 in `pino-http.config.spec.ts`, 3 new pagination-specific tests added to `bookings.service.spec.ts`). This confirms the pagination and structured-logging changes are sound against a real, fully-generated Prisma client, not just the sandbox's `tsc`-only signal. `backend/package-lock.json` picked up a small diff from this real install (npm annotating existing packages with `"peer": true` — a metadata-only difference between npm client versions, no package versions or integrity hashes changed) — committed as-is so the lockfile matches what a real `npm install` on this machine actually produces.
12. ✅ **Clean up the duplicate Prisma migration history (2026-08-10).** `0_baseline`, `20251227114043_add_partner_invoices`, and `20251228212110_add_customer_bills` were all added in the same commit (`856e97b`, "Phase 2.3... Prisma migration baseline, retire legacy raw SQL"). Traced the actual history: the commit message explains `0_baseline` was freshly generated via `prisma migrate diff --from-empty` against the live database and resolved as applied ("prisma migrate status confirms no drift"); the two dated migrations were pre-existing local files that had never been committed (an old `.gitignore` entry excluded `prisma/migrations` entirely) and got swept in alongside the new baseline rather than being reconciled or removed. Confirmed via content diff that `0_baseline` is a strict superset — it creates every enum/table the other two do, plus `tenants`/`customer_bills` they predate, with the corrected schema (`locations.type` as `VARCHAR`, matching the real DB; `add_partner_invoices` still had it as the `location_type` enum, a difference traceable to an archived legacy raw-SQL migration noted in the same commit message). This wasn't just a local-dev shadow-database inconvenience as originally flagged — a genuinely fresh database (new staging environment, rebuilt Docker volume, someone else cloning the repo) running `prisma migrate deploy` from empty would apply `0_baseline` first and then fail outright on `add_partner_invoices` re-creating enums/tables that already exist. **Verified safe before touching anything**: asked the user to run `prisma migrate status` against the real database first (read-only) — confirmed all 7 migrations, including the two redundant ones, already have rows in `_prisma_migrations` marked applied, so nothing here touches business data, only migration bookkeeping. Deleted both redundant folders from git. Confirmed the remaining five (`0_baseline` → `phase4_tenant_id_everywhere` → `user_tenant_memberships` → `equipment_details_json` → `schedule_slot_guides`) have no further overlapping `CREATE TYPE`/`CREATE TABLE` statements, so a from-scratch replay (shadow database, or a genuinely fresh `migrate deploy`) now applies cleanly end to end. **User needs to run one more step**, since it writes to the real database's migration bookkeeping table (not something to execute without direct DB access): `DELETE FROM "_prisma_migrations" WHERE migration_name IN ('20251227114043_add_partner_invoices', '20251228212110_add_customer_bills');` — clears the now-orphaned rows so `prisma migrate dev` stops warning that they're "applied to the database but missing from the local migrations directory." Not required for `migrate deploy`/`status` to keep working (they only check for pending migrations, not orphaned rows), just for a fully clean `migrate dev` experience.
13. ✅ **Fixed a real CI bug caught by the first actual GitHub Actions run (2026-08-10).** The `Frontend (Admin PWA)` job failed on both of the first two real pushes — `Tests` step, exit code 1 — which item 7's roadmap entry had explicitly flagged as unverified in this sandbox. Reproduced locally with a genuinely clean `rm -rf node_modules && npm install --legacy-peer-deps` (matching what CI does on every run): 7 of 11 test suites failed with `Cannot find module '@testing-library/dom'`. Root cause: `@testing-library/user-event@14.6.1` declares `@testing-library/dom` only as a **peerDependency** (`>=7.21.4`), never as a real dependency — it was never listed in `frontend/package.json` either. It happened to work before only because `@testing-library/react@14.3.1`'s own dependency on `@testing-library/dom@^9.0.0` got hoisted to the project root by whatever install produced the previously-committed `node_modules`/lockfile state, which `user-event`'s resolution could reach by coincidence. A fresh install doesn't guarantee that hoisting — and GitHub Actions runners always start from a clean checkout with no pre-existing `node_modules`, so this broke on literally the first real CI run. **This is why the "verify strict DTO validation" work that was about to start got interrupted** — a live, red CI badge on `main` took priority. Fixed by adding `"@testing-library/dom": "^9.0.0"` as an explicit `devDependency` in `frontend/package.json` (same version already in use via the nested copy, so no behavior change, just making the dependency graph correct instead of accidentally-working). Verified: a second from-scratch `rm -rf node_modules && npm install --legacy-peer-deps` plus `CI=true npx react-scripts test --watchAll=false` now passes clean, 80/80, and `react-scripts build` still succeeds. `public-website` has no `@testing-library/*` dependencies or test files at all, so it was never affected. This is exactly the kind of gap the CI pipeline (item 7) exists to catch — it caught a real one on its first real run, just not the one anyone was specifically looking for.

14. ✅ **Verify strict DTO validation against real frontend payloads (2026-08-10).** Item 2's `forbidNonWhitelisted: true` (global `ValidationPipe`, `main.ts`) was flagged at the time as "not yet smoke-tested against the running frontend." It wasn't theoretical risk — auditing every `dataService.create`/`update` call site against its real backend DTO turned up a large number of genuinely broken, live create/update flows. Methodology: for each resource, read the real `Create*Dto`/`Update*Dto` to get the authoritative field list, grep every frontend call site for that resource, compare field names/casing against the DTO, and cross-check the Prisma schema directly whenever a field's existence was in doubt.
    - **Bookings** (`useScheduleData.js`, `useBoatPrepData.js`, `TripDetails.jsx`): four handlers sent `slotAssignment` (never a real column — `slotAssignments`/`slotGuides` display state has always been rebuilt client-side from a field the backend never persisted; that deeper gap is unrelated to this fix and not addressed here) and `notes`/`dietaryRequirements` (also never real `bookings` columns). Fixed by stripping these from every API payload while keeping them as local-only optimistic UI state; API calls are now skipped entirely when nothing real changed.
    - **Whole-object-spread anti-pattern**: `Bookings.jsx` (`{ ...booking, status: 'cancelled' }`), `stayService.js`, `bookingRepricingService.js` (also sent server-managed `updatedAt`), `BillDocument.jsx` (dead `billId`/`billDate`-on-booking loop — no such columns exist; the real bill-to-booking link, `customerBills.bookingIds`, was already correct elsewhere in the same function — deleted the dead loop), and `Prices.jsx` (`{ ...loc, ... }`) all spread a full loaded object that includes fields no DTO declares (`id`, relations, `tenant_id`, timestamps). Fixed by sending only the fields that actually changed. `LocationsManagement.jsx`'s create call also dropped an extra `createdAt` (server-managed).
    - **`boats`/`diveSites` — a more severe class of bug**: `transformBoatToBackend`/`transformDiveSiteToBackend` in `realApiAdapter.js` were sending **snake_case** keys (`location_id`, `equipment_onboard`, `is_active` / `location_id`, `depth_range`, `difficulty_level`, `is_active`) while the actual DTOs require camelCase. This isn't "extra noise" like the bugs above — it meant the *required* `locationId` field was effectively missing under a name the DTO doesn't recognize, so creating or editing any boat or dive site was completely broken. Fixed both to build the correct camelCase shape.
    - **`boatPreps`**: `transformBoatPrepToBackend` sent undeclared `createdAt`/`updatedAt` — always present on an edit of an existing boat prep (real timestamps loaded from the backend), so every edit failed. Removed both fields.
    - **`partnerInvoices`**: checked whether `transformPartnerInvoiceToBackend`'s inclusion of `paidAmount`/`status` (valid only on `UpdatePartnerInvoiceDto`, absent from `CreatePartnerInvoiceDto`) was a live bug — it wasn't, neither field is actually sent by either create call site (`BillDocument.jsx`, `useBillData.js`). Did find a real, separate, pre-existing bug in both: `billId: calculatedBill.billNumber` — the DTO's `billId` is `@IsUUID()` (a FK to a real `customerBills` row), but `billNumber` is a display string (`` `BILL-${Date.now()}` ``), not a UUID, and no `customerBills` row is created in this flow. This has always failed `IsUUID` validation, independent of `forbidNonWhitelisted` — partner-invoice creation from a closed stay has always 400'd. Fixed by omitting `billId` (it's optional; the bill number is already captured in the invoice's `notes` text).
    - **Equipment — the one genuine product gap, not just a payload bug**: the Add/Edit Equipment form has always collected brand, model, thickness, style, hood, purchase date, warranty, last/next revision dates, and (for regulators) first/second-stage + octopus brand/model — none of which ever had a backend column, confirmed against `schema.prisma`. Before `forbidNonWhitelisted` this was a silent no-op (the service's explicit per-field mapping just ignored them); with it on, the whole create/update request 400s instead, and the data has never been persisted either way. Asked the user how to handle it (extend the schema vs. strip the UI fields vs. patch around the crash without saving the data); chose to extend the schema. Added `equipment.details Json? @default("{}")` (same free-form pattern as `customers.preferences`) via a hand-written migration (`20260810180000_equipment_details_json` — this sandbox's `prisma generate`/`migrate dev` can't reach `binaries.prisma.sh`, so it's hand-authored in the same style as prior migrations, e.g. `20260721141428_user_tenant_memberships`), a `details?: Record<string, unknown>` field on both equipment DTOs, and service persistence in `equipment.service.ts`. Added `transformEquipmentToBackend`/`transformEquipmentFromBackend` to `realApiAdapter.js` (bucketing the ~16 non-DTO fields into `details` on the way out, flattening them back onto the root on the way in — same pattern as `transformCustomerToBackend`'s handling of `preferences`) and registered `equipment` in `transformResponse()`, which was previously missing entirely (`getAll('equipment')`/`getById` returned raw, untransformed backend data). `useEquipmentData.js`'s call sites (`handleToggleAvailability`'s `{ ...item, isAvailable: ... }` spread, `handleSaveEquipment`'s raw `formData`) needed no changes — the new transform functions explicitly whitelist known fields rather than passing objects through, so extra fields on a loaded/spread item are simply ignored rather than tripping `forbidNonWhitelisted`.
    - **Verified:** backend 75/75 tests pass, frontend 7/7 relevant suites (20/20 tests) pass, no regressions. Backend `tsc --noEmit` has exactly one error (`equipment.service.ts`'s `details` field unknown to the generated Prisma client) — the same `prisma generate`-blocked-in-sandbox limitation as items 7–9, resolved automatically once `prisma generate`/`migrate dev` run for real on a machine with network access.
    - **Deliberately not attempted**: real backend persistence for Schedule/BoatPrep slot-and-guide assignments (the deeper, pre-existing gap noted above — slot/guide assignments have never survived a page reload, independent of this fix) — that's a bigger feature requiring its own schema/design work, flagged here rather than bundled in (see item 15).
    - **Post-session follow-up (2026-08-10, real Prisma client on the developer's machine):** once `prisma generate`/`migrate dev` ran for real, `tsc --noEmit` surfaced a second-order issue the sandbox's `tsc`-only check couldn't see: `equipment.service.ts`'s `details: dto.details ?? {}` didn't satisfy Prisma's `InputJsonValue` type (the DTO types `details` as `Record<string, unknown>`, stricter than `customers.preferences`'s `any`, which is why an equivalent line there never hit this). Fixed with an explicit `as Prisma.InputJsonValue` cast at the service boundary rather than loosening the DTO's type. Verified clean `tsc --noEmit` and 75/75 backend tests on the real client.

15. ✅ **Real persistence for Schedule's Mole-slot/boat-session and guide assignments (2026-08-10).** Item 14 found that Schedule's `slotAssignments`/`slotGuides` state was entirely rebuilt each load from a `booking.slotAssignment` field with no backing column — never survived a page reload — but scoped that out as a bigger, separate fix. This closes it. Investigation found boat-slot *customer* assignment already persisted correctly via the real `boatId` column (not part of the actual gap); what never persisted was which 30-min Mole time-slot a booking sat in, and which staff cover any slot (Mole or boat) — a guide can be assigned before any customer is, so it can't live on a booking at all.
    - **Design choice, asked explicitly rather than assumed:** Boat Prep already has an overlapping concept (`boat_preps.staff` — captain/guides/trainees, keyed by date+session+boat) serving its own day-of-operations workflow, but at coarser granularity (session, not 30-min slots) and keyed to customer IDs, not bookings. Chose a dedicated schema for Schedule rather than merging into `boat_preps` — self-contained, doesn't touch Boat Prep's already-working code, and actually solves both halves of the gap (a `boat_preps`-merge would only have covered the boat half).
    - **Schema** (migration `20260810210000_schedule_slot_guides`): `bookings.moleSlotTime` (`"HH:mm"`, Mole bookings only) and `bookings.session` (`"morning"`/`"afternoon"`, boat bookings only — same free-text convention as `boat_preps.session`; also fixes a second, smaller pre-existing bug where two same-day trips on the same boat couldn't be told apart, since nothing recorded which session a booking's boat assignment belonged to). New `schedule_slot_guides` table (`location_id`+`date`+`slot_type`+`slot_key` unique, `guide_ids` JSON) — `slot_key` re-uses the same opaque identifier string the frontend already builds for a slot (`"mole-2026-08-10-09-00"`, `"boat-<boatId>-2026-08-10-morning"`) rather than having the backend re-derive it from a UUID-containing hyphenated string.
    - **Backend**: `moleSlotTime`/`session` added to `Create/UpdateBookingDto` and plumbed through `bookings.service.ts`. New `schedule-slot-guides` module (DTOs, service, controller, registered in `app.module.ts`) — deliberately plain CRUD, no bespoke upsert-on-create: the frontend looks up whether a record already exists for a slot and calls `update()` vs `create()` accordingly (same "editing ? update : create" pattern as `useEquipmentData.js`'s `handleSaveTank`), which also means it behaves identically in mock mode (whose generic `create()`/`update()` have no upsert concept either).
    - **Frontend** (`useScheduleData.js`): `slotAssignments` (Mole) now rebuilt from the real `booking.moleSlotTime` instead of the dead field. New `slotGuideRecordIds` state tracks each slot's backing row id. `handleAssignCustomer` persists `moleSlotTime`/`session` instead of doing nothing (Mole) or only touching `boatId` (boat). `handleRemoveAssignment`/`handleRemoveBoatAssignment` clear the same real fields on removal — Mole removal previously made no API call at all. `handleUpdateGuides` now actually persists via `scheduleSlotGuides` instead of being pure local state.
    - **Fixed along the way**: `SlotDetailView.jsx`'s boat guide-select key was `boat-<boatId>-<session>` with **no date** — guides "assigned" to e.g. Boat A's morning session showed as assigned on *every* day, not just the day they were set. Added the date to the key. Also added session-filtering to boat-slot rosters (`(b.session || 'morning') === sessionKey`, backward-compatible default for pre-existing bookings) so two same-day trips on one boat no longer show an identical, merged roster.
    - **`TripDetails.jsx`'s `getGuideNames(booking)` was dead code** — it read `booking.slotAssignment.guideIds`, which was always `undefined` (the field never persisted), so Trip Details has never actually shown assigned guides for any booking, full stop. Rewritten to derive the booking's slot key from its own real fields (`moleSlotTime`, or `boatId`+`session`+`bookingDate`) and look it up in the newly-real `scheduleSlotGuides` list. `handleRemoveDiver` and the per-booking "assignment" dropdown were also only ever touching `boatId` even for Mole bookings (where it's irrelevant) and never touching `session` (now both fixed, branching on `tripType`). `useBoatPrepData.js`'s boat-assignment sync effect now also writes `session` alongside `boatId`, keeping Boat Prep's writes consistent with Schedule's.
    - **Verified:** backend 47/47 Prisma-independent tests pass in-sandbox; two new test files (`bookings.service.spec.ts` moleSlotTime/session cases, new `schedule-slot-guides.service.spec.ts`) can't run here (same stale-sandbox-Prisma-client limitation as items 7–9/14 — needs `prisma generate` on a real machine to verify, same as those). Frontend: full suite 80/80 passing, `react-scripts build` compiles clean. Manually re-verified `tsc --noEmit` shows only the expected new-schema-vs-stale-client errors (`mole_slot_time`, `session`, `schedule_slot_guides` — all resolve once the real client is regenerated), nothing else.
    - **Deliberately out of scope**: unifying this with `boat_preps` (see design choice above); a `bookings.moleSlotTime`/`session` migration path for rows created before this change (they simply have no slot assignment yet, same as any other newly-added optional column — no backfill needed).

16. ✅ **`react-router` v6→v7 upgrade, closing the deferred moderate open-redirect advisory (2026-08-10).** Item 1 deferred this pending the CRA→Vite migration (item 4), assuming it needed bundling with a bigger change. Checked properly before deciding: no patched 6.x release exists at all (latest is `6.30.4`, itself inside one of the three advisories' vulnerable range) — `7.18.0+` is the only real fix, so "defer indefinitely" wasn't actually an option, only "defer how long." Asked the user to choose between doing it now vs. sticking to the original deferral; chose now, informed by having already checked that both apps (`frontend`, `public-website`) only use basic declarative-mode APIs (`BrowserRouter`/`HashRouter`, `Routes`, `Route`, `useNavigate`, `useLocation`, `useParams`, `useSearchParams`, `Navigate`) with no relative-path `<Link>`/`navigate()` calls beneath the one splat route (`/*` in `App.jsx`) and no in-component `React.lazy()` calls — the two behavior changes the official v6→v7 guide calls out (`v7_relativeSplatPath`, `v7_startTransition`) are no-ops for this codebase. React 18.2 (both apps) meets v7's `>=18` peer requirement.
    - Bumped `react-router-dom` `^6.20.0`/`^6.22.0` → `^7.18.2` in both `frontend` and `public-website` (kept the `react-router-dom` package name rather than migrating to the unified `react-router` package the guide frames as optional final cleanup — `react-router-dom@7.18.2` depends on the patched `react-router@7.18.2` internally, so the fix applies either way, and skipping the package rename means zero import changes across ~18 files in each app). `public-website`'s install needed `--legacy-peer-deps` (a pre-existing, unrelated `typescript` peer conflict from `react-scripts`, already the documented pattern the CI workflow uses for both frontend and public-website installs — not something this change introduced).
    - **Found two Jest-only compatibility gaps, both fixed, neither a real app bug:** `react-router-dom@7`'s entry point does `require('react-router/dom')`, a package.json `"exports"` subpath map CRA's bundled Jest resolver doesn't follow (confirmed via a clean `react-scripts build` succeeding throughout — this only ever broke `react-scripts test`, never the actual webpack build) — fixed with a `moduleNameMapper` in `frontend/package.json`'s `"jest"` block pointing `react-router/dom` at the resolved file directly. Second: `react-router@7`'s internals reference `TextEncoder`/`TextDecoder`, which CRA's Jest/jsdom environment doesn't polyfill globally the way a real browser (or webpack's own build output) does — fixed by polyfilling both from Node's `util` module in `frontend/src/setupTests.js`. `public-website` has no test files at all (a pre-existing gap noted in item 7), so neither fix was needed there, but will be if tests are ever added — flagging here rather than adding untested, unwired Jest config for tests that don't exist yet.
    - **Verified:** `npm audit` on both apps confirms zero `react-router`/`react-router-dom` findings (was 1 finding each, moderate). Frontend: full suite 80/80 passing (was already 80/80 before this — the two Jest fixes restored suites that the v7 bump had broken, no net change), `react-scripts build` compiles clean. `public-website`: `react-scripts build` compiles clean (no tests to run).
    - **Not done**: migrating either app off `react-router-dom` onto the unified `react-router` package name (deliberately skipped, see above — no security or functional benefit, only import churn); `public-website`'s `HashRouter`→`BrowserRouter` switch (a separate, already-listed item 3, unrelated to this security fix).

17. ✅ **NestJS 10→11 upgrade, closing the deferred moderate `@nestjs/core` injection advisory (2026-08-11).** Item 1 flagged this and suggested it might be best resolved together with the CRA→Vite migration (item 4); checked properly rather than assuming — the two are unrelated (Vite affects the frontend build tool, this is a backend framework major version), so no reason to keep bundling them. Latest stable is `11.1.29` (`12.x` is alpha-only, not a real target yet). Audited every v10→v11/Express v5 breaking change against this codebase before touching anything: all ~40 route decorators use plain `:param` segments (no wildcards, no regex, no optional `?` modifiers) so Express v5's stricter `path-to-regexp` v8 doesn't affect them; `setGlobalPrefix('api')` in `main.ts` is a plain string, not the now-unsupported RegExp form; every `@Query()` usage is a scalar named param, none rely on Express v5's changed nested-object query parser default; no `CacheModule`/`cache-manager` usage; `tsconfig.json` already uses plain `commonjs` (not `node16`/`bundler`), so the DI-container module-resolution change doesn't apply; no multer/file-upload usage to be affected by `platform-express`'s bundled `multer` bump. `@nestjs/jwt`/`@nestjs/passport`/`@nestjs/config`/`@nestjs/throttler`/`nestjs-pino`/`pino-http` were already on versions whose peer ranges accept `^11.0.0`, so no changes needed there.
    - Bumped `@nestjs/common`/`@nestjs/core`/`@nestjs/platform-express` `^10.3.0` → `^11.1.29`, `@nestjs/config` `^3.1.1` → `^4.0.4`, dev-only `@nestjs/cli` `^10.2.1` → `^11.0.24`, `@nestjs/schematics` `^10.0.3` → `^11.1.0`, `@nestjs/testing` `^10.3.0` → `^11.1.29`, `@types/express` `^4.17.21` → `^5.0.6` (matches `platform-express@11`'s now-default bundled `express@5.2.1`).
    - **Found a live, current supply-chain regression while verifying, not a hypothetical:** `@nestjs/swagger@11.4.6` (the actual latest patch) bundles `js-yaml@5.2.1`, which has its own fresh high-severity DoS advisory; pinning back to `@nestjs/swagger@11.4.5` swaps that for `js-yaml@4.3.0` instead — which turned out to be independently vulnerable too, to a *different*, newer high-severity CVE (quadratic CPU consumption in `!!omap` resolution, fixed in `4.3.1`, "fix not backported" to earlier 4.x). Neither available `@nestjs/swagger` patch version ships a clean `js-yaml`. Fixed with a package.json `"overrides"` block pinning `js-yaml` to `^4.3.1` across the whole dependency tree (verified this actually works: `npm ls js-yaml` after a genuinely clean install shows every consumer, including the one nested inside `@nestjs/swagger`, deduped onto the single overridden `4.3.1` copy — an npm quirk found along the way: the override didn't take effect on an incremental `npm install` over stale `node_modules`, only after `rm -rf node_modules package-lock.json` first). `4.3.1` sits outside both vulnerable ranges (the 5.x DoS regression only affects `5.0.0–5.2.1`; the omap CVE only affects `<4.3.1`) and matches the version every other dependency in the tree (`cosmiconfig`, `eslint`, `puppeteer`) already resolves to on its own, so this isn't introducing a new/unusual version into the tree.
    - **Verified end-to-end in-sandbox, not just researched** — this session's Prisma-binary network block (documented in items 7/8/9) turned out not to be a blocker here: the real, already-generated Prisma client from a prior local `prisma generate` was already present in the mounted `node_modules/.prisma/client` (darwin-arm64, matching items 14/15's schema), so it could be copied into a scratch install and used for real type-checking rather than hitting the usual 42-stub-error wall. In a from-scratch `npm install --ignore-scripts` scratch copy: `tsc --noEmit` clean, `nest build` clean, full suite **90/90 passing**, `npm audit` shows zero `@nestjs/*`-related findings (was 1 moderate). Full app boot against a live Postgres connection was not exercised (no database reachable from this sandbox) — worth a quick `npm run start:dev` smoke check on your machine before treating this as fully proven, though `tsc`/build/tests passing clean is a strong signal.
    - **Not done**: the `12.0.0-alpha.x` line (not stable yet, no reason to track it); resolving the unrelated pre-existing `@typescript-eslint`/`minimatch` high-severity dev-tooling findings surfaced by the same `npm audit` run (a `@typescript-eslint` 6→8 major bump, ESLint dev dependency only, never shipped to production — out of scope for this item, flagged here as a real but separate gap).

18. ✅ **`frontend` migrated off Create React App onto Vite (2026-08-11), closing Phase 5 item 4 for the admin app.** Asked the user to scope this before starting, given it's the biggest single change of this session: `frontend` first (it has a real test suite and a CI job to verify against) rather than both apps at once; `public-website` deliberately deferred as a smaller follow-up. Full Jest→Vitest swap chosen over keeping Jest running separately, matching the Vite ecosystem convention.
    - **Toolchain:** `vite@8.2.1`, `@vitejs/plugin-react@6.0.5`, `vitest@4.1.10`, `jsdom@30.0.1` (replacing `react-scripts@5.0.1` entirely — removed, not kept alongside). `vite-plugin-pwa@1.3.0` replaces CRA's built-in `workbox-webpack-plugin` + `src/serviceWorkerRegistration.js`: it now auto-generates the manifest (from a `manifest` option in `vite.config.js`, translated field-for-field from the old `public/manifest.json`) and auto-injects the service-worker registration script, rather than the app calling `.register()` itself. Output filenames changed to the plugin's defaults (`sw.js`, `manifest.webmanifest`, not CRA's `service-worker.js`/`manifest.json`) — updated everywhere they were hardcoded: `frontend/docker/nginx.conf`'s two `location` blocks.
    - **Entry point moved to convention:** root-level `index.html` (was `public/index.html`, referencing `%PUBLIC_URL%`) → `src/index.jsx` (was `index.js` — needed the `.jsx` extension since it contains real JSX and Vite's esbuild only parses JSX in `.js` files if explicitly configured to, which this deliberately avoids in favor of matching the file to its real content). `frontend/public/` no longer exists — it held only `index.html` and `manifest.json`, both now Vite-native/plugin-generated. (Found along the way, not caused by this change: the manifest's icon references, `logo192.png`/`logo512.png`/`favicon.ico`, were never actually present anywhere in the repo, before or after — a pre-existing gap, not a regression, left as-is since generating real PWA icons wasn't in scope here.)
    - **Four more `.js` files renamed to `.jsx`** for the same JSX-parsing reason (`utils/authContext.js`, `utils/languageContext.js`, `utils/partnerAuthContext.js`, `hooks/useFinancialData.js`, `hooks/useBoatPrepData.js`, plus `utils/authContext.test.js`) — found via a full-repo scan for JSX syntax inside `.js` files, not just guessed. No import statements needed updating (none in this codebase specify an explicit `.js` extension, so bundler extension resolution just works).
    - **Env vars renamed `REACT_APP_*` → `VITE_*`** (Vite only exposes `VITE_`-prefixed vars to client code) across every real reference: `src/config/apiConfig.js`, `src/components/Settings/PartnersManagement.jsx`, `src/services/breachService.js`, `.env.example`, `src/config/README.md`, `frontend/Dockerfile` (`ARG`/`ENV`), `deploy/zbox/k8s/admin.yaml`'s comment, `docs/DOCKER_VPS_LOCAL_TEST.md`'s troubleshooting section. No local `.env` file existed to update (only `.env.example` was ever committed). `process.env.NODE_ENV === 'development'` in `syncService.js` → `import.meta.env.DEV` (Vite's native equivalent).
    - **Jest→Vitest, 10 of 11 test files touched:** mechanical `jest.mock/fn/spyOn/clearAllMocks` → `vi.mock/fn/spyOn/clearAllMocks` across the board. Two non-mechanical fixes found by actually running the suite, not anticipated in advance: (1) `jest.requireActual(...)` spread inside 4 `vi.mock()` factories (`Customers`/`Equipment`/`Financial`/`Settings.smoke.test.jsx`, all mocking `authContext` partially) — Jest's `requireActual` is sync, Vitest's equivalent (`vi.importActual`) is async, so the factories became `async () => ({ ...(await vi.importActual(...)), ... })` (a documented, standard Vitest migration pattern, not improvised). (2) `ProtectedRoute.test.jsx`'s two component mocks (`vi.mock('./AdminLogin', () => () => <div>...)`) failed at runtime with "is not returning an object" — Jest's CJS-interop mocking tolerated a factory returning the component function directly as if it were the default export; Vitest's stricter ESM-based mocking requires the literal shape (`() => ({ default: () => <div>... })`). Both fixes verified by actually re-running the suite afterward, not assumed correct from reading the Vitest docs.
    - **`--legacy-peer-deps` no longer needed for `frontend`** — that flag was working around a `typescript`/`react-scripts` peer conflict; with `react-scripts` gone, a plain `npm install` resolves clean, verified via a genuinely fresh install (0 vulnerabilities, was 30 high/moderate findings under CRA per item 1). Removed from `frontend/Dockerfile` and the CI workflow's frontend job. *(**Update, item 19:** turned out `public-website` didn't need it either, same underlying cause — see that item.)*
    - **Node version bumped 18→22** in `frontend/Dockerfile` and `.github/workflows/ci.yml`'s frontend job — the old Node 18 pin was specifically a `react-scripts 5.0.1` compatibility workaround (documented in both places at the time); Vite 8 actually requires Node `>=20.19` or `>=22.12`, so this wasn't optional once CRA was gone, and 18 would have failed outright.
    - **Real, not hypothetical, regression avoided:** CRA provided ESLint-based linting for free during `start`/`test`/`build` (warnings, and build-failing under `CI=true`); dropping `react-scripts` drops that too, since this repo has no standalone `.eslintrc`/`eslintConfig` of its own. **Flagging this as a real gap, not silently absorbing it** — a proper Vite-era ESLint setup (flat config, `eslint-plugin-react-hooks`, etc.) is a legitimately separate task, not attempted here.
    - **Verified end-to-end, twice** — once in a scratch copy while building the migration out, then again from a genuinely clean checkout of the actual committed changes (`rm -rf node_modules build package-lock.json && npm install`), to catch anything that only existed in the scratch copy by accident: `npm install` clean (0 vulnerabilities, both times), `vite build` clean (`build/sw.js`, `build/manifest.webmanifest`, `build/index.html` all present and correctly linked; 30 lazy-loaded page chunks — same code-splitting behavior as item 10, main entry chunk 97KB vs. the old 176KB main bundle), full suite **80/80 passing** (same count as before the migration — no test coverage lost or silently skipped), `vite preview` serves the built output with `sw.js`/`manifest.webmanifest` both returning 200, and `vite`'s own dev server boots clean. CI workflow (`.github/workflows/ci.yml`) updated to match (`npx vitest run`, `npx vite build`, Node 22) but the actual GitHub Actions run itself is the next real verification, same caveat as items 7/13.
    - **Not done**: `public-website`'s CRA→Vite migration (deliberately deferred, see above — smaller app, no tests, separate follow-up); a proper post-CRA ESLint setup (flagged above as a real gap); generating the PWA's missing icon files (pre-existing gap, not caused by this change).
    - **Post-item follow-up (2026-08-11):** two real bugs surfaced only once run against the user's actual machine, not caught by this session's own verification. (1) **CI itself was broken** on the first real push — `package.json` was updated in this commit but `package-lock.json` never regenerated to match; `npm install` against the stale, still-react-scripts-shaped lockfile produced a genuine ERESOLVE conflict (old `babel-preset-react-app` chain vs. new `@vitejs/plugin-react`'s peer deps). Every verification in this item deleted `package-lock.json` before installing, which passes regardless of what's actually committed — it never tested what CI actually does (install against the real committed lockfile). Fixed by regenerating from a genuinely clean state and verifying a second install cycle against the fresh lockfile is stable. (2) **The test suite failed entirely on the user's machine** (`window.localStorage.clear is not a function`, 71/80 tests) — root cause: Node 25.x (only 25.x — the user's `node -v`) ships a native `localStorage` global, unflagged and on by default, that's an unusable stub unless `--localstorage-file=<path>` is given; it shadows jsdom's own working implementation ([nodejs/node#57666](https://github.com/nodejs/node/pull/57666), fixed properly in Node 26; a known, actively-tracked ecosystem-wide break, not specific to this repo — [vitest-dev/vitest#8757](https://github.com/vitest-dev/vitest/issues/8757)). **First attempted fix was wrong**, worth recording so it isn't repeated: assumed it was `jsdom@30`'s own engine-range gap (`^22.22.2 || ^24.15.0 || >=26.0.0` excludes odd "current" Node releases) and pinned `jsdom` down to `26.1.0` — shipped that as a commit, asked the user to re-test, and it made no difference (still 71/80 failing), because the real cause was Node's own global, not anything jsdom-version-specific. Corrected fix: conditional `poolOptions.{forks,threads}.execArgv: ['--no-webstorage']` in `vitest.config.js`, gated on `process.versions.node` major `=== 25` (checked via a scratch Node-25-flag test: `--no-webstorage` is flatly unrecognized — "bad option" — on Node 22, so this cannot be a blanket flag in the `test` script or CI without breaking every other Node version; also confirmed `NODE_OPTIONS=--no-webstorage` specifically is rejected by Node's env-var allowlist even where the flag itself exists, so it has to be `execArgv`, not `NODE_OPTIONS`). Reverted the `jsdom` pin back to `^30.0.1` since it was never the actual problem. **Not verified against the actual failure** — this sandbox's Node is 22.x, which doesn't have the bug, so the `nodeMajor === 25` branch could only be verified by code inspection and the documented fix from the upstream issue, not by reproducing the failure and watching it go green here. Confirmed only that the fix is a no-op and doesn't break anything on Node 22 (80/80 still passing, clean build, stable across two install cycles). Genuinely needs the user's own machine to confirm. **(3) That "corrected" fix was itself broken and never actually ran**, per the user's next re-test: it nested `execArgv` under `poolOptions.{forks,threads}`, a shape Vitest 4 removed outright (`test.poolOptions` was removed — every former `poolOptions.<pool>` option, including `execArgv`, is now a top-level `test` option; confirmed by fetching the real migration guide, https://vitest.dev/guide/migration#pool-rework, rather than guessing again). Vitest printed a `DEPRECATED` warning and silently ignored the whole block, so the Node-25 flag was never actually passed and the failure was byte-for-byte identical to before. Fixed by moving `execArgv` to a plain top-level key of the `test` object (removing `poolOptions` entirely — nothing else in this config needs it). Re-verified in-sandbox: 80/80 passing, no deprecation warning, config parses clean. **Still not verified against the real Node 25 failure** — same caveat as before, needs the user's machine.

19. ✅ **`public-website` migrated off Create React App onto Vite (2026-08-11), same day as item 18, closing Phase 5 item 4 fully.** Smaller and simpler than `frontend`'s migration in most ways (no test suite to port), but with one real structural difference worth calling out on its own.
    - **No `vite-plugin-pwa`, unlike `frontend`** — `public-website/public/service-worker.js` is a real, hand-authored service worker (network-first for HTML/JS/CSS so deploys reach users immediately without a stale cache, cache-first for static assets, background-sync and push-notification handlers), not CRA's auto-generated workbox precache output that `frontend` had. Swapping it for plugin-generated output would have silently changed real, deliberate caching/deploy behavior. Correct fix: nothing needed — Vite's `public/` directory convention copies files straight through to the build output untouched, exactly like CRA's `public/` did, so `service-worker.js`, `manifest.json` (kept as `manifest.json`, not renamed — no plugin involved to rename it to `.webmanifest`), `logo.svg`, `pwa-icons/`, and `.htaccess` (Apache config for the OVH FTP-hosted production path) all just keep working as-is. The existing `src/utils/serviceWorkerRegistration.js` (this app's own hand-written registration logic, separate from `frontend`'s now-removed one) was kept too, just with its two `process.env.PUBLIC_URL` references switched to `import.meta.env.BASE_URL`.
    - **Real, non-cosmetic bug found and fixed:** `src/services/bookingService.js`'s `deleteCustomerAccount()` used a bare CJS `require('./anonymizationService')` inside the function body. Webpack (CRA) tolerates `require()` in browser code via its CJS interop; Vite's ESM build output does not — this would have thrown `ReferenceError: require is not defined` in the browser the first time a customer actually tried to delete their account (a GDPR-mandated deletion flow), silently unexercised by anything until then since there's no test coverage for this app. Checked whether it was a genuine circular-import workaround first (`anonymizationService.js` imports nothing, so no) before converting it to a real `await import('./anonymizationService')` — `deleteCustomerAccount` was already `async`, so this was a clean fit, not a workaround of its own.
    - **`REACT_APP_API_URL` → `VITE_API_URL`**, larger footprint than `frontend`'s (7 service files reference it directly: `syncService.js`, `dsarService.js`, `bookingService.js` ×11 occurrences, `auditService.js`, `packPurchaseService.js`, `consentServiceAPI.js`, `breachService.js`) plus `.env.example`. `process.env.NODE_ENV === 'production'` in `index.js` → `import.meta.env.PROD`. **Found and fixed the same rename in four places item 18 didn't touch, because they're outside `frontend/`:** `deploy/deploy_landing_to_ovh.py` (the actual OVH FTP production deploy script — builds `public-website` with this var set, so a stale name here would have silently shipped the wrong (or no) API URL to production), `deploy/ovh/sync-build-deploy.sh` and `deploy/zbox/deploy.sh`/`sync-build-deploy.sh` (Docker build-args for both `frontend` and `public-website` — item 18 only fixed `frontend`'s own `Dockerfile`, not these three deploy scripts that pass the same build-arg name to it from outside, a real gap item 18 left that this item closes), `backend/ADMIN_LOGIN.md`, `start-network.sh` (also dropped its `HOST`/`PORT` env vars here, since those were a CRA convention and don't control Vite's dev server the way they controlled `react-scripts` — noted as a pre-existing, separate port-labeling inconsistency in that script's banner text, not fixed here).
    - **`eslintConfig: { extends: ['react-app'] }` removed from `package.json`** — dead config now that nothing reads it without `react-scripts`; same ESLint-on-build gap item 18 already flagged for `frontend` applies here too, not a new one.
    - **`index.js` → `index.jsx`** (contains JSX), same reason as item 18's renames. Root-level `index.html` replaces `public/index.html`, stripped of `%PUBLIC_URL%` placeholders (plain `/` paths work the same way in Vite's output).
    - **`--legacy-peer-deps` no longer needed here either** (same root cause as item 18 — the `typescript`/`react-scripts` peer conflict is gone with `react-scripts`), removed from `Dockerfile` and the CI workflow. Node bumped 18→22 in both, same reason as item 18 (Vite 8 requires `>=20.19`/`>=22.12`).
    - **One pre-existing, unrelated `npm audit` finding surfaced, not fixed:** `sharp` (high severity, fixed-available only via a semver-major bump) — it's a devDependency used solely by the standalone `generate-icons` script (regenerates PWA icon PNGs from source art, not part of the build or anything shipped to users), unrelated to this migration, left alone rather than bundling an untested major-version bump into this change.
    - **Verified:** genuinely clean install (`rm -rf node_modules build package-lock.json && npm install`) — 0 ERESOLVE errors, only the pre-existing `sharp` finding. `vite build` succeeds; output verified to contain `service-worker.js`, `manifest.json`, `logo.svg`, `pwa-icons/`, `.htaccess`, and `LOGO_PLACEMENT.md` all copied through unchanged, plus the correctly-injected module script tag in `build/index.html`. (One pre-existing characteristic surfaced by Vite's build output, not a regression: a single ~677KB JS chunk, since this app has no route-based code-splitting the way `frontend` got in item 10 — flagging as a possible future improvement, not done here.) `vite`'s dev server boots clean. No test suite exists to run (pre-existing gap, item 7) — the `require()` fix above could not be exercised by an automated test for that reason, only verified by confirming the build compiles it without error; a manual click-through of the actual account-deletion flow against a real backend is worth doing before this ships.
    - **Not done**: adding any test coverage to `public-website` (pre-existing gap, out of scope here); the `sharp` major-version bump; a post-CRA ESLint setup (same flagged gap as item 18); route-based code-splitting for the one large chunk noted above; `HashRouter`→`BrowserRouter`/SEO work (separate, already-listed item 3).

## Sequencing notes

- Phases 0–1 can start immediately and in parallel (different files, no schema risk).
- Phase 2 depends on phase 0 being deployed (don't tighten validation on endpoints that are still unauthenticated in prod).
- Phase 3 must exist before phase 4 — adding `tenant_id` everywhere without an isolation test suite risks shipping the exact bug being fixed, just moved.
- Phase 5 has no hard dependency on 3–4 and can be picked up whenever capacity allows.
