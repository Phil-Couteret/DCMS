PORT=3003
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
DATABASE_URL=postgresql://user@localhost:5432/dcms?schema=public

# Required. App refuses to start without this. Generate with: openssl rand -hex 32
JWT_SECRET=

# Optional
LOCAL_NETWORK_IP=
RESET_ADMIN_USER=
RESET_ADMIN_PASSWORD=
TENANT_SLUG=
