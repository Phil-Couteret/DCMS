# Optional. If unset, apiConfig.js derives the backend URL from the current
# hostname. Set this to override, e.g. for local development against a
# specific backend:
VITE_API_URL=http://localhost:3003/api
