server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        # Optional: proxy to backend when behind same nginx in prod
        return 404;
    }
}
